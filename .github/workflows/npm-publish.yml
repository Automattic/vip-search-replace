name: Publish to npm (if applicable)

on:
  pull_request:
    types: [closed]

permissions:
  contents: read

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    environment: npm-publish
    if: contains( github.event.pull_request.labels.*.name, '[ Type ] NPM version update' ) && startsWith( github.head_ref, 'release/') && github.event.pull_request.merged == true
    permissions:
      contents: write
      id-token: write
      pull-requests: write
    steps:
      - uses: Automattic/vip-actions/npm-publish@438cd00aac2463a2dbfb4a747ad7b1ac67a2575a # v0.7.5
        with:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USE_TRUSTED_PUBLISHING: 'true'
          PROVENANCE: 'true'
          CONVENTIONAL_COMMITS: 'true'
          SKIP_BUMP_TO_DEV: 'true'
          TAG_PREFIX: 'v'
