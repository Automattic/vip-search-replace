---
name: Prepare new npm release

on:
  workflow_dispatch:
    inputs:
      npm-version-type:
        description: 'The npm version type we are publishing.'
        required: true
        type: choice
        default: 'patch'
        options:
          - patch
          - minor
          - major

permissions:
  contents: read

jobs:
  prepare:
    name: Prepare a new npm release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Check out the source code
        uses: actions/checkout@v6

      - name: Run npm-prepare-release
        uses: Automattic/vip-actions/npm-prepare-release@08981f10d85134a309ae47bf590b5e8ea5172d46 # v0.7.3
        with:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          npm-version-type: ${{ inputs.npm-version-type }}
          conventional-commits: 'true'
